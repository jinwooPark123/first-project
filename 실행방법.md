# 프로젝트 실행 방법

## 프로젝트 구조
- **Backend**: Flask 서버 (Python) - 포트 5000
- **Frontend**: React 앱 (Node.js) - 포트 3000

---

## 1단계: Backend 설정 및 실행

### 1.1 가상환경 활성화
```bash
cd backend
venv\Scripts\activate
```

### 1.2 환경 변수 설정
`backend` 폴더에 `.env` 파일을 생성하고 다음 내용을 추가하세요:
```
OPENAI_API_KEY=your_openai_api_key_here
```

### 1.3 필요한 패키지 설치 (이미 설치되어 있을 수 있음)
```bash
pip install -r requirements.txt
```

### 1.4 Flask 서버 실행
```bash
python app.py
```

서버가 `http://127.0.0.1:5000`에서 실행됩니다.

---

## 2단계: Frontend 설정 및 실행

### 2.1 새 터미널 창 열기
Backend 서버가 실행 중인 상태에서 **새 터미널 창**을 엽니다.

### 2.2 Frontend 디렉토리로 이동
```bash
cd frontend
```

### 2.3 의존성 설치 (처음 한 번만)
```bash
npm install
```

### 2.4 React 앱 실행
```bash
npm start
```

브라우저가 자동으로 열리고 `http://localhost:3000`에서 앱이 실행됩니다.

---

## 요약

**터미널 1 (Backend):**
```bash
cd backend
venv\Scripts\activate
python app.py
```

**터미널 2 (Frontend):**
```bash
cd frontend
npm start
```

---

## 주의사항
- Backend 서버가 먼저 실행되어야 Frontend가 정상 작동합니다.
- `.env` 파일에 유효한 OpenAI API 키가 있어야 AI 기능이 작동합니다.
- 두 서버가 모두 실행된 상태에서 브라우저에서 `http://localhost:3000`에 접속하세요.

